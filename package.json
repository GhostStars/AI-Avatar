{
  "name": "ai-avatar-factory-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "AI avatar generation MVP with a small Node.js server.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0"
  },
  "engines": {
    "node": ">=20"
  }
}
